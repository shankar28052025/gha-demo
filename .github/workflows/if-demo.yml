name: If demo
on:
 push:
   branches: 
     - main
     - develop
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: run if branch is main
        if: github.ref == 'refs/heads/main'
        run: echo "run this for main branch"
      - name: run without condition
        run: echo "run this step"
