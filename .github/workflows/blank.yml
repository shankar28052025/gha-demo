name: Hello World!
on: workflow_dispatch
jobs:
  build: 
     runs-on: ubuntu-latest
     steps:
       - name: build step
         run: echo "demo build"
  test:
     runs-on: ubuntu-latest
     needs: build
     steps:
       - name: test phase
         run: echo "running test  cases"
  deploy:
      runs-on: ubuntu-latest
      needs: test
      steps:
        - name: deploy phase
          run: echo "deploy the app"
